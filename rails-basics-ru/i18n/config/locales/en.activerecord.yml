en:
  activerecord:
    attributes:
      # BEGIN
      post:
        title: Title
        body: Body
      # END
      post/comment:
        body: Body

    models:
      # BEGIN
      post: Posts
      # END
      post/comment: Comment

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: should be filled
            body:
              max_length: The body must not exceed 500 characters.
            # END
