ru:
  activerecord:
    attributes:
      post:
        # BEGIN
        title: Название
        body: Содержание
        # END
      post/comment:
        body: Содержание

    models:
      # BEGIN
      post: Посты
      # END
      post/comment: Комментарий

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: Должно быть заполнено
            body:
              max_length: Содержимое не должно превышать 500 символов.
            # END
